import { useState, type FormEvent } from 'react';
import { Button } from '../../../shared/components/Button';
import { Field } from '../../../shared/components/Field';
export function InvestmentValuationForm({ onCancel, onSubmit }: { onCancel: () => void; onSubmit: (v: Record<string, unknown>) => Promise<void> }) {
  const [date, setDate] = useState(new Date().toISOString().slice(0, 10)); const [totalValue, setTotalValue] = useState(''); const [unitPrice, setUnitPrice] = useState(''); const [source, setSource] = useState('Manual'); const [notes, setNotes] = useState(''); const [saving, setSaving] = useState(false);
  async function submit(e: FormEvent) { e.preventDefault(); setSaving(true); try { await onSubmit({ date, totalValue: Number(totalValue), ...(unitPrice ? { unitPrice: Number(unitPrice) } : {}), source: source || undefined, notes: notes || undefined }); } finally { setSaving(false); } }
  return <form onSubmit={submit} className="space-y-4"><Field label="Fecha"><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="w-full rounded-lg border border-line bg-white px-3 py-2 text-sm" required /></Field><Field label="Valor total"><input type="number" min="0.01" step="0.01" value={totalValue} onChange={e=>setTotalValue(e.target.value)} className="w-full rounded-lg border border-line bg-white px-3 py-2 text-sm" required /></Field><Field label="Precio por unidad"><input type="number" min="0.000001" step="0.000001" value={unitPrice} onChange={e=>setUnitPrice(e.target.value)} className="w-full rounded-lg border border-line bg-white px-3 py-2 text-sm" /></Field><Field label="Fuente"><input value={source} onChange={e=>setSource(e.target.value)} className="w-full rounded-lg border border-line bg-white px-3 py-2 text-sm" /></Field><Field label="Notas"><textarea value={notes} onChange={e=>setNotes(e.target.value)} rows={3} className="w-full rounded-lg border border-line bg-white px-3 py-2 text-sm" /></Field><div className="flex justify-end gap-2"><Button type="button" variant="secondary" onClick={onCancel}>Cancelar</Button><Button type="submit" disabled={saving}>{saving?'Guardando...':'Guardar valoración'}</Button></div></form>;
}
